{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
  "build": "pnpm -r --if-present run build",
  "start": "pnpm -r --filter artifacts start"
  }
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
