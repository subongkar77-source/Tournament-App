{
  "name": "workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "start": "pnpm --filter @workspace/api-server start"
  },
  "devDependencies": {
    "pnpm": "latest"
  }
}

